# Disabled for root-published a-gent.co deployments.
#
# These host-specific rewrites were only needed when Netlify published the dashboard/ directory.
# The production a-gent.co site now publishes from the repository root (.) so the root index.html
# must be served directly. Keeping the old forced rewrites would send a-gent.co back to
# dashboard/marketing/index.html and hide the current Book Demo / Outbound A-Gent updates.
